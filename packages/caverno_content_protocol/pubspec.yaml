name: caverno_content_protocol
description: Pure-Dart parsing contracts for Caverno LLM response content.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.8

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.0
