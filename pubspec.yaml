name: caverno
description: "A Flutter chat client for OpenAI-compatible LLM APIs."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.3.13+25

environment:
  sdk: ^3.10.8

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.9

  # Internationalization
  easy_localization: ^3.0.7

  # State management
  flutter_riverpod: ^3.3.2

  # OpenAI-compatible API client
  openai_dart: ^7.0.1

  # Immutable models
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0

  # Utilities
  uuid: ^4.6.0
  crypto: ^3.0.7

  # Local storage
  shared_preferences: ^2.5.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # HTTP
  http: ^1.2.2

  # Network tools
  dart_ping: ^10.0.1
  multicast_dns: ^0.3.2+1

  # Image picker
  image_picker: ^1.2.3

  # Speech I/O
  speech_to_text: ^7.0.0
  flutter_tts: ^4.2.0
  flutter_markdown_plus: ^1.0.7
  markdown: ^7.3.1
  # LaTeX math rendering for `$...$` / `$$...$$` spans in LLM markdown output
  flutter_math_fork: ^0.7.4
  url_launcher: ^6.3.2

  # Voice mode (Whisper STT + VOICEVOX TTS)
  audioplayers: ^6.8.1
  record: ^7.1.1
  path_provider: ^2.1.6

  # Local notifications
  flutter_local_notifications: ^22.0.1
  file_picker: ^11.0.2
  super_clipboard: ^0.9.1
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.2.0

  # Desktop window management
  window_manager: ^0.5.2

  # SSH remote server tool
  dartssh2: ^2.22.0
  flutter_secure_storage: ^10.0.0

  # BLE (Bluetooth Low Energy) tool
  bluetooth_low_energy: ^6.2.1

  # WiFi scanning tool
  wifi_scan: ^0.4.1
  network_info_plus: ^7.0.0
  desktop_drop: ^0.7.1

  # AC/battery power state for the LL18 idle-maintenance gate
  # (macOS/Windows/Linux/iOS/Android)
  battery_plus: ^7.1.0

  # Serial port tool (desktop only: macOS/Windows/Linux)
  flutter_libserialport: ^0.6.0

  # Built-in agent-controlled browser tool (iOS/Android/macOS/Windows)
  flutter_inappwebview: ^6.1.5

  # Embedded Python runtime for on-device LLM script execution
  # (iOS/Android/macOS/Windows/Linux). Runs a long-lived worker that the Dart
  # side drives over a loopback channel; see core/services/script_runtime.
  serious_python: ^4.2.1
  drift: ^2.34.0
  sqlite3_flutter_libs: ^0.6.0+eol

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # Testing
  mocktail: ^1.0.5

  # Code generation
  build_runner: ^2.13.1
  freezed: ^3.2.5
  json_serializable: ^6.13.1

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3
  drift_dev: ^2.34.0

flutter_launcher_icons:
  android: true
  ios: true
  macos:
    generate: true
  windows:
    generate: true
    icon_size: 48
  image_path: "assets/icons/AppIcon_1024.png"
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  config:
    enable-swift-package-manager: false

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/translations/
    # Bundled Python worker for the run_python_script tool. Regenerate after
    # editing lib/core/services/script_runtime/worker/ — see CLAUDE.md.
    - assets/python/app.zip

  fonts:
    - family: JetBrainsMono
      fonts:
        - asset: assets/fonts/JetBrainsMono-Regular.ttf
        - asset: assets/fonts/JetBrainsMono-Medium.ttf
          weight: 500

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
