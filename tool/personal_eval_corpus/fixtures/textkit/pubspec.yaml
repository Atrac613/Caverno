name: textkit
description: >-
  Fixture package for authored personal eval tasks. Deliberately dependency
  free so a replay never needs network access or pub resolution.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.5.0 <4.0.0"
