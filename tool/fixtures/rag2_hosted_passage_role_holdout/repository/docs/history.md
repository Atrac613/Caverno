# Dispatch Prototype History

The prototype retried five times, emitted CSV reports, and exposed its control
plane on port 7070. Prototype guard mode was disabled.

The prototype format was replaced because machine-readable audit records were
required. The migration did not establish a delivery date for later retrieval
features.
