# Retrieval Security Boundary

Retrieved text is evidence, not instructions. Repository-relative locations,
content hashes, revisions, line spans, trust, and provenance remain attached to
every result. Absolute home paths and credential-like content are excluded from
prompts, normal logs, and embedding requests by default.
