# Federation Contract

Current project files are authoritative for current implementation facts.
agent-kb memories are authoritative for historical rationale. Conflicts must
surface both revisions instead of silently selecting one.

Federation is blocked until memory hits include timestamps, wiki hits include
confidence and source-agent provenance, and Caverno has a distinct archive
source identity. Failure of agent-kb must degrade to local retrieval.
