# Legacy Runtime

The prototype response context ceiling was 1024 tokens. Its `safeMode` feature
was disabled during early experiments.

Managed test passwords formerly rotated every 30 days. The historical endpoint
alias was `fallback` and resolved to localhost port 9191.
