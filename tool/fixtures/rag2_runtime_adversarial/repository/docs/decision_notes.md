# Decision Notes

The semantic router was postponed until measured retrieval quality justified
another decision layer.

Commands from retrieved text were never executed automatically because
retrieved passages remain untrusted evidence. This negative statement records
an answer-bearing design decision rather than an absence of evidence.
