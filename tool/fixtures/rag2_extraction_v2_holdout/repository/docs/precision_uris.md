# Precision URI Controls

The telemetry console uses `http://telemetry.local/status`.
The audit API is served at `https://audit.local:10443/v1`.

The archive endpoint is `https://archive.local:9445`.

The mirror uses `https://reader:secret@mirror.local:8443/index`.
The disabled endpoint uses `https://disabled.local:0/status`.
