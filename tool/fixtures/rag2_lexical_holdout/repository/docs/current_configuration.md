# Current Configuration

The current response token ceiling is 4096 and the default sampling
temperature is 0.7. The built-in MCP endpoint defaults to
`http://localhost:8081`.

Voice transcription defaults to the Whisper-compatible service at
`http://localhost:8080`. Conversation persistence uses drift on SQLite.
