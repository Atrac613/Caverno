# Superseded Configuration

These values describe old builds and are not authoritative now.

- The response token ceiling was once 2048.
- The default sampling temperature was formerly 0.2.
- The MCP endpoint previously used `http://localhost:9090`.
- Conversations were stored in Hive before the drift migration.
