# Endpoint Reference

The dashboard is published at `https://localhost:7443`. Its current build
number is 8081.

The webhook callback uses `https://localhost/callback`. Deployment notes
mention port 9443 for a separate service but do not assign it to the callback.
