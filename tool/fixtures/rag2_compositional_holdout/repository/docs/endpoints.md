# Endpoint Topology

The admin console is served at `https://admin.local:7443`. The metrics console
is served at `https://metrics.local:9555`.

The proxy at `https://proxy.local:8080` forwards to the API at
`https://api.local:9090`. The public portal uses
`https://portal.local/docs` over standard HTTPS transport.
