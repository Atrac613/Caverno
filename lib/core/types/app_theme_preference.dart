/// Which of Caverno's two themes to paint the app with.
///
/// Dark is the designed-for default (see `DESIGN.md`); [system] follows the
/// platform brightness so a light-mode desktop is not blinded at night.
enum AppThemePreference { system, dark, light }
